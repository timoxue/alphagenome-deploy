# 用户账号信息

## 预设用户账号

AlphaGenome JupyterHub 已配置以下用户账号：

### 管理员账号

| 用户名 | 密码 | 权限 | 说明 |
|--------|------|------|------|
| `admin` | `admin123` | 管理员 | 可以管理所有用户和服务器 |

### 普通用户账号

| 用户名 | 密码 | 权限 | 说明 |
|--------|------|------|------|
| `user1` | `user123` | 普通用户 | 团队成员 1 |
| `user2` | `user123` | 普通用户 | 团队成员 2 |
| `user3` | `user123` | 普通用户 | 团队成员 3 |
| `user4` | `user123` | 普通用户 | 团队成员 4 |
| `user5` | `user123` | 普通用户 | 团队成员 5 |

## 首次登录步骤

1. 打开浏览器访问：`http://localhost:8000`（或服务器IP:8000）
2. 输入用户名和密码
3. 点击"Sign In"登录

## 安全建议

### ⚠️ 生产环境部署前请修改密码！

在 `config/jupyterhub_config.py` 文件中修改密码：

```python
self.users = {
    'admin': 'your_secure_password_here',     # 修改管理员密码
    'user1': 'user1_custom_password',          # 修改用户1密码
    'user2': 'user2_custom_password',          # 修改用户2密码
    # ... 其他用户
}
```

修改后需要重启服务：
```bash
docker-compose restart
```

## 添加新用户

如需添加更多用户，在 `config/jupyterhub_config.py` 中：

1. 在 `self.users` 字典中添加新用户：
   ```python
   'user6': 'password123',  # 新用户
   ```

2. 在 `c.JupyterHub.load_roles` 中添加用户：
   ```python
   {
       'name': 'users',
       'users': ['user1', 'user2', 'user3', 'user4', 'user5', 'user6'],  # 添加 user6
       # ...
   }
   ```

3. 重启服务

## 用户空间隔离

每个用户登录后都有独立的：
- ✅ `~/work/` - 个人工作空间
- ✅ `~/work/results/` - 分析结果
- ✅ `~/work/data/` - 个人数据
- ✅ `~/work/notebooks/` - 个人 notebook

所有用户共享：
- ✅ `/shared/notebooks/` - 示例教程（只读）
- ✅ `/shared/data/` - 参考数据（只读）
- ✅ `/shared/tools/` - 工具库（只读）

## 管理员功能

使用 `admin` 账号登录可以：
- 查看所有用户
- 管理用户服务器
- 访问管理面板
- 停止/启动任意用户的服务器

## 技术支持

如有问题：
1. 检查服务状态：`docker-compose ps`
2. 查看日志：`docker-compose logs -f`
3. 查看文档：`README_CN.md` 或 `USER_GUIDE_CN.md`

---

**重要提示**：
- 🔒 这些密码仅用于测试
- 📝 生产环境请立即修改
- 🔑 建议使用强密码
- 👥 根据实际团队调整用户数量
